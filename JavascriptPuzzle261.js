/* Write a function which accept number as argument and return the sum of all number */

// using the Big - O Calculation

function summation(n) {
  return (n * (n + 1)) / 2;
}

console.log(summation(10)); // 55