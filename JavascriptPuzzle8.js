let numbers = [1, 2, 3, 4, 5];

const mapNumbers = numbers.map(e => e > 3);

console.log(mapNumbers);

const filterNumbers = numbers.filter(e => e > 3);

console.log(filterNumbers);

const findNumbers= numbers.find(e => e > 3);

console.log(findNumbers);
