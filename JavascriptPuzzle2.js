// What is the output of the following code snippet and why ?

const str = "script";

const paddStr = str.padStart(10, "java-");

console.log(paddStr);

// Pad start will add beginning characters accroding to position passed in the first parameter.

